import React from 'react';
import PropTypes from 'prop-types';

import './Cloud.scss';

const Cloud = (props) => {
  return (
    <div className="cloud--body" />
  );
};

Cloud.propTypes = {
};

export default Cloud;
